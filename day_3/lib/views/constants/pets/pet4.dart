import 'package:flutter/material.dart';

class Pet4 extends StatelessWidget {
  const Pet4({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Page 4'),
      ),
    );
  }
}
